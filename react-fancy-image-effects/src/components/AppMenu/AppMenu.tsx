import * as React from 'react';
import {List, ListItem, ListItemText} from '@mui/material';

export const AppMenu = () => (
    <List>
        <ListItem>
            <ListItemText>Column Fade</ListItemText>
        </ListItem>
    </List>
);